import React from 'react';      //imports react from React library

export class Footer extends React.Component { //'export' keyword allows exportation & access to components (etc.) to other js files

    render(){
        return(
            <div>
                <h1>This is the footer component</h1>
            </div>
        );
    }
}